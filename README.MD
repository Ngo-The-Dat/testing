# NETWORKING WITH WINSOCK
---

**Build:**
```
cd ./TCP-Redo
./build all

// or ./build client
// or ./build server
```

**Run server:**
```
// from TCP
cd ./server
./server.exe <ip> <port>
```

**Run client:**
```
// from TCP
cd ./client
./client.exe <ip> <port>
```